/* import 'package:flame_test/flame_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seriousgame/d_game_scenes/game_scene_generator.dart';
// 


final diabeteGameTester = FlameTester(DiabeteGameScene.new);
// TODO: Antony - Test Exemple
void main() {
  group('Hud tests', () {
    TestWidgetsFlutterBinding.ensureInitialized();

    diabeteGameTester.testGameWidget('Hud is loaded',
        setUp: (game, tester) async {
      await game.ready();
      //  await game.hud.loaded;
    }, verify: (game, tester) async {
      //  expect(game.hud.isLoaded, true);
    });
  });
}
 */